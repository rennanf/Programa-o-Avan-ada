 01. Seja o seguinte trecho de programa:

    int i=3,j=5;
    int *p, *q;
    p = &i;
    q = &j;

Determine o valor das seguintes expressões:

p == &i; --> Compara se o valor que está armazenado em p é igual ao endereço da varivável i, e isso é correto, logo, a resposta é true ou 1.

*p - *q; --> Diminuindo o conteúdo armazanado em cada variável , o resultado será (3-5) = -2.

**&p; --> Retorna o valor armazenado em P. Como ele recebe o valor de i, será 3.

3 - *p/(*q) + 7; --> Dividimos os valores armazenados na memória dos ponteiros p e q, como são inteiros, a divisão resultará em 0, logo, restará a soma dos valores 3+7 = 10. 
